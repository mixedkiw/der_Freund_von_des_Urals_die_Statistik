# 🚀 Развёртывание на GitHub Pages

## Инструкция по развёртыванию фронтенда на GitHub Pages

### 1. **Предварительные требования**
- GitHub аккаунт
- Git установлен локально
- Node.js и npm установлены

### 2. **Создание репозитория на GitHub**

1. Перейди на https://github.com/new
2. Создай новый репозиторий с именем: **`username.github.io`**
   - Замени `username` на своё имя пользователя GitHub
   - Например: `kacher.github.io`
3. **Важно**: Репозиторий должен быть **public**
4. Инициализируй без README, .gitignore и лицензии

### 3. **Настройка локального проекта**

```bash
# Перейди в папку frontend
cd /home/kacher/Projects/Project/frontend

# Инициализируй git (если ещё не инициализирован)
git init

# Добавь удалённый репозиторий
git remote add origin https://github.com/YOUR_USERNAME/YOUR_USERNAME.github.io.git

# Проверь конфигурацию
git remote -v
```

### 4. **Обнови vite.config.js для правильного пути на GitHub Pages**

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  base: '/',  // Корневая папка репозитория github.io
  server: {
    port: 5173
  }
})
```

### 5. **Соберись и загрузи на GitHub Pages**

```bash
# Установи зависимости (если не установлены)
npm install

# Собери production версию
npm run build

# Инициализируй git (если первый раз)
git config user.email "your_email@example.com"
git config user.name "Your Name"

# Добавь все файлы
git add .

# Коммит
git commit -m "Initial commit: Deploy frontend to GitHub Pages"

# Загрузи на GitHub
git branch -M main
git push -u origin main

# Публикуй папку dist на GitHub Pages
npm run build
# Создай ветку gh-pages с содержимым dist/
# Вариант 1: Вручную через GitHub Actions (см. ниже)
# Вариант 2: Используй gh-pages пакет
```

### 6. **Автоматическая публикация с GitHub Actions (Рекомендуется)**

Создай файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Build
        run: |
          cd frontend
          npm run build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./frontend/dist
```

После создания этого файла, каждый push в `main` ветку будет автоматически собирать и публиковать фронтенд на GitHub Pages!

### 7. **Проверь развёртывание**

1. Перейди в настройки репозитория → Pages
2. Убедись, что "Source" установлен на "gh-pages" ветка
3. После 1-2 минут сайт будет доступен на: `https://YOUR_USERNAME.github.io`

### 8. **Обнови API URL в .env файлах**

**frontend/.env.production.local:**
```
VITE_API_URL=https://your-api-url.com/api
```

Замени `https://your-api-url.com` на реальный URL твоего API (например, ngrok URL или Cloudflare Tunnel).

---

## 📝 Часовые пояса

✅ **Хорошая новость:** Логика часовых поясов работает корректно!

- Время активации интерпретируется как **Yekaterinburg (UTC+5)**
- Конвертируется в **UTC** на сервере
- На клиенте (GitHub Pages) сравнивается с текущим **UTC временем**
- **Не зависит** от часового пояса пользователя или GitHub Pages

Текущее время активации: **2025-12-10 08:00:00** (Yekaterinburg)

Для изменения измени значение в `src/pages/SuccessPage.vue`:
```javascript
activationDateTime: '2025-12-10 08:00:00', // Измени на нужное время
```

---

## 🔗 Ссылки

- GitHub Pages Документация: https://docs.github.com/en/pages
- Vite документация: https://vitejs.dev/
- Vue.js документация: https://vuejs.org/

---

## ❓ Возможные проблемы

### "404 при открытии сайта на GitHub Pages"
- Проверь, что репозиторий **public**
- Проверь настройки Pages в репозитории (Settings → Pages)
- Убедись, что в `vite.config.js` правильно установлен `base: '/'`

### "API запросы не работают (CORS ошибка)"
- GitHub Pages может блокировать запросы к твоему API
- Используй CORS proxy или Cloudflare Workers
- Или используй Ngrok для проксирования твоего API

### "Картинки и шрифты не загружаются"
- Проверь пути в коде (должны быть относительными)
- Убедись, что файлы есть в папке `public/` или `src/assets/`

---

## 🎉 Готово!

После выполнения этих шагов твой фронтенд будет доступен на GitHub Pages, и IP адрес сервера не будет палиться! 🚀
