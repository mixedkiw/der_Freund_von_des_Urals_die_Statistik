# 🏗️ Архитектура Express.js проекта

## Структура папок

```
Project/
├── frontend/          ← Фронтенд (Vue.js)
│   ├── index.html
│   ├── package.json
│   ├── src/
│   │   ├── App.vue
│   │   ├── main.js
│   │   └── components/
│   └── public/
│
└── backend/           ← Бэкенд (Express.js) ✅ НОВЫЙ
    ├── src/
    │   ├── index.js              ← Запуск сервера
    │   ├── app.js                ← Конфигурация
    │   ├── swagger.json          ← Документация API
    │   └── routes/
    │       ├── users.js          ← CRUD пользователей
    │       └── products.js       ← CRUD продуктов
    │
    ├── node_modules/             ← Установленные пакеты
    ├── package.json              ← Зависимости
    ├── package-lock.json         ← Версии пакетов
    │
    ├── README.md                 ← Основная документация
    ├── QUICKSTART.md             ← Быстрый старт
    ├── LEARNING_GUIDE.md         ← Учебный гайд
    └── test-api.sh               ← Примеры curl команд
```

## Как работает API

### 1️⃣ Запуск сервера

```javascript
// src/index.js
const app = require('./app');
const PORT = 3000;

app.listen(PORT, () => {
  console.log(`Сервер запущен на порту ${PORT}`);
});
```

**Команда для запуска:**
```bash
npm start
```

### 2️⃣ Конфигурация приложения

```javascript
// src/app.js
const express = require('express');
const app = express();

// ↓ Middleware (обработчики)
app.use(express.json());           // Парсит JSON
app.use(swaggerUi.serve);          // Документация

// ↓ Маршруты
app.use('/api/users', usersRouter);
app.use('/api/products', productsRouter);

module.exports = app;
```

### 3️⃣ Маршруты (Routes)

```javascript
// src/routes/users.js
const router = express.Router();

// GET /api/users
router.get('/', (req, res) => {
  res.json({ users });
});

// POST /api/users
router.post('/', (req, res) => {
  const newUser = req.body;
  res.status(201).json(newUser);
});

// PUT /api/users/:id
router.put('/:id', (req, res) => {
  const updated = { id: req.params.id, ...req.body };
  res.json(updated);
});

// DELETE /api/users/:id
router.delete('/:id', (req, res) => {
  res.json({ deleted: true });
});

module.exports = router;
```

## Диаграмма потока запроса

```
┌─────────────────────────────────────────────────────────────┐
│                        CLIENT (Браузер)                     │
│                                                              │
│  curl http://localhost:3000/api/users                      │
│  POST http://localhost:3000/api/users (+ JSON данные)      │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ↓ HTTP Запрос
┌─────────────────────────────────────────────────────────────┐
│                    EXPRESS SERVER (index.js)                │
│                                                              │
│  app.listen(3000)                                           │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                   APP CONFIGURATION (app.js)               │
│                                                              │
│  app.use(express.json())    ← Middleware                    │
│  app.use('/api/users', usersRouter)                         │
│  app.use('/api/products', productsRouter)                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ↓ Поиск маршрута
┌─────────────────────────────────────────────────────────────┐
│                    ROUTER (routes/users.js)                 │
│                                                              │
│  router.get('/', handler)           ← /api/users            │
│  router.post('/', handler)          ← создание             │
│  router.put('/:id', handler)        ← обновление           │
│  router.delete('/:id', handler)     ← удаление             │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ↓ Обработка
┌─────────────────────────────────────────────────────────────┐
│              HANDLER FUNCTION (в маршруте)                  │
│                                                              │
│  (req, res) => {                                            │
│    const data = req.body;  ← Получаем данные              │
│    // ... логика ...                                        │
│    res.json(data);         ← Отправляем JSON              │
│  }                                                          │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ↓ HTTP Ответ
┌─────────────────────────────────────────────────────────────┐
│                    CLIENT (Браузер)                         │
│                                                              │
│  Status: 200 OK                                             │
│  Body: { "success": true, "data": [...] }                  │
└─────────────────────────────────────────────────────────────┘
```

## Типичный CRUD цикл

### Создание (CREATE) - POST
```
CLIENT                          SERVER
   │                              │
   ├─── POST /api/users ────────→ │
   │     { name, email, age }    │
   │                              │ app.js
   │                              │  ↓
   │                              │ router.post('/', (req, res) => {
   │                              │   const newUser = req.body
   │                              │   users.push(newUser)
   │                              │   res.status(201).json(newUser)
   │                              │ })
   │                              │
   │ ←────── 201 Created ──────── │
   │  { id: 4, name, email, age } │
   │                              │
```

### Чтение (READ) - GET
```
CLIENT                          SERVER
   │                              │
   ├─── GET /api/users/1 ───────→ │
   │                              │ router.get('/:id', (req, res) => {
   │                              │   const user = users.find(...)
   │                              │   res.json(user)
   │                              │ })
   │                              │
   │ ←────── 200 OK ────────────→ │
   │  { id: 1, name, email, age } │
   │                              │
```

### Обновление (UPDATE) - PUT
```
CLIENT                          SERVER
   │                              │
   ├─── PUT /api/users/1 ──────→  │
   │     { name: "Иван Новый" }  │
   │                              │ router.put('/:id', (req, res) => {
   │                              │   const user = users.find(...)
   │                              │   user.name = req.body.name
   │                              │   res.json(user)
   │                              │ })
   │                              │
   │ ←────── 200 OK ────────────→ │
   │  { id: 1, name: "Иван Новый" │
   │                              │
```

### Удаление (DELETE) - DELETE
```
CLIENT                          SERVER
   │                              │
   ├─── DELETE /api/users/1 ────→ │
   │                              │ router.delete('/:id', (req, res) => {
   │                              │   users.splice(...)
   │                              │   res.json({ deleted: true })
   │                              │ })
   │                              │
   │ ←────── 200 OK ────────────→ │
   │  { deleted: true }           │
   │                              │
```

## HTTP Методы и Статус Коды

| Операция | Метод | URL | Статус |
|----------|-------|-----|--------|
| Получить | GET | `/api/users` | 200 OK |
| Получить один | GET | `/api/users/1` | 200 OK |
| Создать | POST | `/api/users` | 201 Created |
| Обновить | PUT | `/api/users/1` | 200 OK |
| Удалить | DELETE | `/api/users/1` | 200 OK |
| Ошибка (не найдено) | ANY | ANY | 404 Not Found |
| Ошибка (валидация) | ANY | ANY | 400 Bad Request |
| Ошибка (сервер) | ANY | ANY | 500 Server Error |

## Файлы и их роль

### 📄 src/index.js
**Что делает:** Запускает Express сервер на порту 3000

```javascript
const app = require('./app');
app.listen(3000, () => {
  console.log('Сервер запущен!');
});
```

### 📄 src/app.js
**Что делает:** Конфигурирует Express приложение

```javascript
const express = require('express');
const app = express();

app.use(express.json());  // Middleware
app.use('/api/users', usersRouter);

module.exports = app;
```

### 📄 src/routes/users.js
**Что делает:** Определяет API endpoints для пользователей

```javascript
const router = express.Router();

router.get('/', handler);      // Получить всех
router.get('/:id', handler);   // Получить одного
router.post('/', handler);     // Создать
router.put('/:id', handler);   // Обновить
router.delete('/:id', handler);// Удалить

module.exports = router;
```

### 📄 src/swagger.json
**Что делает:** Содержит документацию API в OpenAPI формате

```json
{
  "swagger": "2.0",
  "info": { "title": "API", "version": "1.0.0" },
  "paths": {
    "/users": {
      "get": { "summary": "Получить пользователей" }
    }
  }
}
```

## URL Структура

```
http://localhost:3000/api/users

┌─────────────────────────────────┐
│ Protocol: http                  │
│ Host: localhost                 │
│ Port: 3000                      │
│ Base path: /api                 │
│ Resource: /users                │
└─────────────────────────────────┘

Полный URL: http://localhost:3000/api/users
```

## Параметры Запроса

### URL Параметры (Path)
```
GET /api/users/123

↓ В коде

req.params.id  // = "123"
```

### Query Параметры (Строка запроса)
```
GET /api/users?page=2&limit=10

↓ В коде

req.query.page   // = "2"
req.query.limit  // = "10"
```

### Body Параметры (Тело запроса)
```
POST /api/users
{ "name": "Иван", "email": "ivan@example.com" }

↓ В коде

req.body.name   // = "Иван"
req.body.email  // = "ivan@example.com"
```

## Ответ API

```javascript
// Успешный ответ
res.status(200).json({
  success: true,
  data: { id: 1, name: 'Иван' },
  count: 1
});

// Ошибка
res.status(404).json({
  success: false,
  error: 'Пользователь не найден'
});
```

---

**Этот проект - идеальная база для обучения Express.js! 🎓**
