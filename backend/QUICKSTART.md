# 🚀 Express.js Проект успешно создан!

## ✅ Что было установлено и создано

### Зависимости:
- ✅ **express** - веб-фреймворк для Node.js
- ✅ **swagger-ui-express** - красивая документация API

### Структура проекта:
```
backend/
├── src/
│   ├── index.js              # Точка входа (запуск сервера)
│   ├── app.js                # Конфигурация Express приложения
│   ├── swagger.json          # OpenAPI документация
│   └── routes/
│       ├── users.js          # API для управления пользователями
│       └── products.js       # API для управления продуктами
├── package.json              # Файл зависимостей Node.js
├── README.md                 # Документация проекта
├── LEARNING_GUIDE.md         # Подробный гайд по Express.js
├── test-api.sh               # Примеры curl команд для тестирования
└── node_modules/             # Установленные пакеты
```

## 🎯 Основные концепции, которые ты изучишь

### 1. **Как создать Express приложение**
```javascript
const express = require('express');
const app = express();

app.use(express.json());  // Middleware для JSON

app.listen(3000, () => {
  console.log('Сервер запущен на порту 3000');
});
```

### 2. **HTTP методы (CRUD операции)**
- **GET** - прочитать данные
- **POST** - создать данные
- **PUT** - обновить данные
- **DELETE** - удалить данные

### 3. **Структура маршрутов**
```javascript
// src/routes/users.js
const router = express.Router();

router.get('/', (req, res) => {
  res.json({ users: [] });
});

router.post('/', (req, res) => {
  const newUser = req.body;
  res.status(201).json(newUser);
});

module.exports = router;
```

### 4. **Обработка параметров**
```javascript
// URL параметры: /api/users/1
req.params.id

// Query параметры: /api/users?page=2
req.query.page

// Body параметры: POST с JSON
req.body
```

### 5. **Ответы API**
```javascript
res.json({ data: [] });              // JSON ответ
res.status(201).json(newUser);       // С кодом состояния
res.status(404).json({ error: '' }); // Ошибка
```

## 🌐 Доступные Endpoints

| Метод | URL | Описание |
|-------|-----|---------|
| GET | `/api/status` | Проверка статуса сервера |
| GET | `/api/users` | Получить всех пользователей |
| GET | `/api/users/:id` | Получить пользователя по ID |
| POST | `/api/users` | Создать пользователя |
| PUT | `/api/users/:id` | Обновить пользователя |
| DELETE | `/api/users/:id` | Удалить пользователя |
| GET | `/api/products` | Получить все продукты |
| GET | `/api/products/:id` | Получить продукт по ID |
| POST | `/api/products` | Создать продукт |
| PUT | `/api/products/:id` | Обновить продукт |
| DELETE | `/api/products/:id` | Удалить продукт |

## 🚀 Как запустить

### Запуск сервера:
```bash
cd /home/kacher/Projects/Project/backend
npm start
```

Сервер будет запущен на: **http://localhost:3000**

### Документация API (Swagger UI):
```
http://localhost:3000/api-docs
```

В Swagger UI ты можешь:
- Видеть все доступные endpoint'ы
- Читать описание каждого
- Тестировать API прямо из браузера!

## 📝 Примеры запросов

### Получить список пользователей:
```bash
curl http://localhost:3000/api/users
```

### Создать пользователя:
```bash
curl -X POST http://localhost:3000/api/users \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван",
    "email": "ivan@example.com",
    "age": 25
  }'
```

### Обновить пользователя:
```bash
curl -X PUT http://localhost:3000/api/users/1 \
  -H "Content-Type: application/json" \
  -d '{"name": "Иван Новый", "age": 26}'
```

### Удалить пользователя:
```bash
curl -X DELETE http://localhost:3000/api/users/1
```

## 📚 Документация

### Главные файлы для изучения:
1. **LEARNING_GUIDE.md** - подробное объяснение всех концепций
2. **README.md** - документация проекта
3. **src/app.js** - как организуется Express приложение
4. **src/routes/users.js** - пример реализации CRUD операций

## 🎓 Как работает проект

### Поток запроса:
```
1. Клиент отправляет запрос
   curl http://localhost:3000/api/users

2. Express получает запрос и ищет подходящий маршрут
   app.use('/api/users', require('./routes/users'));

3. Маршрут обрабатывает запрос
   router.get('/', (req, res) => {
     res.json(users);
   });

4. Сервер отправляет JSON ответ
   {
     "success": true,
     "data": [...],
     "count": 3
   }
```

## 🔧 Что дальше?

### Следующие шаги для развития:
1. **Изучить Express**
   - Middleware (обработчики запросов)
   - Валидация данных
   - Обработка ошибок

2. **Добавить базу данных**
   - MongoDB для документо-ориентированной БД
   - PostgreSQL для реляционной БД

3. **Аутентификация и авторизация**
   - JWT токены
   - Защита маршрутов

4. **Тестирование**
   - Jest для unit тестов
   - Supertest для API тестов

5. **Развертывание**
   - Docker контейнеры
   - Cloud платформы

## 💡 Полезные команды

```bash
# Установить новый пакет
npm install <имя-пакета>

# Запустить сервер
npm start

# Остановить сервер
Ctrl + C

# Выполнить тесты
npm test

# Установить dev зависимость
npm install --save-dev <имя-пакета>
```

## 🔗 Полезные ссылки

- [Express.js Official Docs](https://expressjs.com/)
- [Node.js API Reference](https://nodejs.org/api/)
- [REST API Best Practices](https://restfulapi.net/)
- [Swagger/OpenAPI](https://swagger.io/)
- [HTTP Status Codes](https://httpwg.org/specs/rfc7231.html#status.codes)

---

**Проект полностью готов к использованию! Тебе нужно просто запустить `npm start` и начать изучать. Удачи! 🎉**
