# 🚀 Интеграция Frontend + Backend

## ✅ Статус: ПОЛНОСТЬЮ РАБОЧАЯ СИСТЕМА

### 📊 Архитектура

```
Frontend (Vue.js)      ←→  Backend (Express.js + SQLite)
Port: 5174                 Port: 3000
.env: VITE_API_URL         .env: CORS_ORIGIN, DB_STORAGE
```

---

## 🗄️ База Данных (SQLite)

### Таблицы

**Users**
- `id` (PRIMARY KEY)
- `name` (STRING)
- `createdAt` (DATE)

**Questions**
- `id` (PRIMARY KEY)
- `u_id` (FOREIGN KEY → Users.id)
- `name` (STRING)
- `description` (TEXT)
- `createdAt` (DATE)

**Answers**
- `id` (PRIMARY KEY)
- `user_id` (FOREIGN KEY → Users.id)
- `question_id` (FOREIGN KEY → Questions.id)
- `description` (TEXT)
- `createdAt` (DATE)

---

## 🔌 API Endpoints

### 1. Проверка статуса
```
GET /api/status
```
**Ответ:**
```json
{
  "status": "OK",
  "message": "Сервер работает корректно!",
  "timestamp": "2025-12-09T12:45:54.366Z"
}
```

### 2. Сохранение ответов
```
POST /api/save
Content-Type: application/json

{
  "name": "Иван Петров",
  "answers": [
    {"question": "Вопрос 1", "answer": "Ответ 1"},
    {"question": "Вопрос 2", "answer": "Ответ 2"},
    {"question": "Вопрос 3", "answer": "Ответ 3"}
  ]
}
```

**Ответ (201 Created):**
```json
{
  "success": true,
  "message": "Ответы успешно сохранены",
  "data": {
    "userId": 1,
    "userName": "Иван Петров",
    "answersCount": 3
  }
}
```

### 3. Получение отчёта (CSV)
```
GET /api/get-report
```

**Ответ:** CSV файл с данными всех пользователей и их ответами

```
ID,Имя,Вопрос,Ответ
1,"Иван Петров","Вопрос 1","Ответ 1"
1,"Иван Петров","Вопрос 2","Ответ 2"
```

---

## 🚀 Запуск

### Backend
```bash
cd /home/kacher/Projects/Project/backend
npm start
```
Доступен на: `http://localhost:3000`
Swagger UI: `http://localhost:3000/api-docs`

### Frontend
```bash
cd /home/kacher/Projects/Project/frontend
npm run dev
```
Доступен на: `http://localhost:5174`

---

## ⚙️ Переменные окружения

### Backend (.env)
```
NODE_ENV=development
PORT=3000
DB_DIALECT=sqlite
DB_STORAGE=./database.sqlite
CORS_ORIGIN=http://localhost:5174
API_BASE_URL=http://localhost:3000
```

### Frontend (.env)
```
VITE_API_URL=http://localhost:3000/api
```

---

## 🔒 Безопасность

✅ **CORS** - правильно настроена для кросс-доменных запросов
✅ **JSON парсинг** - валидация входных данных
✅ **.env файлы** - конфиденциальная информация не в коде
✅ **Error handling** - правильная обработка ошибок на клиенте и сервере

---

## 📁 Структура проекта

```
Project/
├── backend/
│   ├── src/
│   │   ├── index.js                 ← Точка входа сервера
│   │   ├── app.js                   ← Конфигурация Express + CORS
│   │   ├── models/
│   │   │   ├── User.js              ← Модель пользователя
│   │   │   ├── Question.js          ← Модель вопроса
│   │   │   ├── Answer.js            ← Модель ответа
│   │   │   └── index.js             ← Связи между моделями
│   │   ├── controllers/
│   │   │   ├── answersController.js ← POST /api/save
│   │   │   └── reportController.js  ← GET /api/get-report
│   │   ├── routes/
│   │   │   └── api.js               ← Маршруты
│   │   ├── database/
│   │   │   ├── connection.js        ← Подключение к БД
│   │   │   └── init.js              ← Инициализация БД
│   │   └── swagger.json             ← Документация
│   ├── database.sqlite              ← База данных
│   ├── .env                         ← Конфигурация
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   └── QuestionsPage.vue    ← Форма с отправкой на backend
│   │   ├── components/
│   │   │   └── CustomInput.vue      ← Input компонент
│   │   ├── App.vue
│   │   └── main.js
│   ├── .env                         ← VITE_API_URL
│   └── package.json
```

---

## 🧪 Тестирование

### Тест POST запроса
```bash
curl -X POST http://localhost:3000/api/save \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовый Пользователь",
    "answers": [
      {"question": "Вопрос 1", "answer": "Ответ 1"}
    ]
  }'
```

### Тест GET отчета
```bash
curl http://localhost:3000/api/get-report
```

### Тест в Swagger UI
Перейди на `http://localhost:3000/api-docs`

---

## 📝 Примеры использования Frontend

### Форма автоматически:
1. Считывает ответы из input полей
2. Первое поле = имя пользователя
3. Остальные = ответы на вопросы
4. При клике на кнопку отправляет JSON на backend
5. Получает ответ и показывает сообщение об успехе/ошибке

---

## 🔧 Возможные ошибки

### CORS ошибка
**Решение:** Убедись, что `CORS_ORIGIN` в backend .env совпадает с адресом frontend

### "Cannot find module"
**Решение:** `npm install` в нужной папке

### Port уже в использовании
**Решение:** Измени PORT в .env или `npx kill-port 3000`

---

## ✨ Особенности

✅ ORM Sequelize - удобная работа с БД
✅ SQLite - встроенная БД, не нужны дополнительные сервисы
✅ CORS правильно настроена
✅ CSV отчеты с правильной кодировкой UTF-8
✅ Адаптивные стили на frontend
✅ Валидация данных
✅ Error handling
✅ Swagger документация

---

## 🎉 Статус: ГОТОВО К ИСПОЛЬЗОВАНИЮ!

Всё работает корректно. Можешь начинать использовать систему! 🚀
